cache:
  paths:
  - app/vendor/

stages:
  - build
  - deploy

build:
  stage: build
  script:
  - docker rmi nimmis/apache-php7
  - docker-compose build

deploy:
  stage: deploy
  script:
  - docker-compose down --remove-orphans
  - docker-compose up -d
