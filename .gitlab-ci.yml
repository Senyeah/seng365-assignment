cache:
  paths:
  - app/vendor/

stages:
  - build
  - deploy

build:
  stage: build
  script:
  - docker-compose build --no-cache

deploy:
  stage: deploy
  script:
  - docker-compose down --remove-orphans
  - docker-compose up --force-recreate
